# Visual Gallery

Here you can see exactly how the pipes look inside the game, both with the custom resource pack enabled (Modeled) and disabled (Vanilla). 

## Modeled vs Vanilla Comparison

Drag the vertical line left and right to compare how pipes look with the **Resource Pack** (Modeled) vs **Vanilla** (ArmorStands).

<script setup>
import ImageCompare from '../.vitepress/components/ImageCompare.vue'
</script>

<ImageCompare
  leftSrc="/Transport-Pipes/images/modeled_pipe.png"
  rightSrc="/Transport-Pipes/images/vanilla_pipe.png"
  leftLabel="📦 Modeled (Resource Pack)"
  rightLabel="🧱 Vanilla (ArmorStands)"
/>

